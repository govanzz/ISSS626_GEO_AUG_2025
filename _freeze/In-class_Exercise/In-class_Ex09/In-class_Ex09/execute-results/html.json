{
  "hash": "99bb6fe5bde1b0385e8a3dc24078ab1e",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"In class Ex09\"\nauthor: \"Gautamgovan\"\ndate: \"November 8, 2025\"\ndate-modified: \"last-modified\"\nformat: \n  html:\n    toc: true\n    toc-depth: 4\n    number-sections: true\nexecute: \n  eval: true\n  echo: true\n  warning: false\n  freeze: true\n---\n## Objective\n\nIn this hands-on exercise, we will gain hands-on experience on how to model geographical accessibility by using R’s geospatial analysis packages.\n\n## The Data\n\nFour data sets will be used in this hands-on exercise, they are:\n\n-   MP14_SUBZONE_NO_SEA_PL: URA Master Plan 2014 subzone boundary GIS data. This data set is downloaded from data.gov.sg.\n\n-   hexagons: A 250m radius hexagons GIS data. This data set was created by using st_make_grid() of sf package. It is in ESRI shapefile format.\n\n-   ELDERCARE: GIS data showing location of eldercare service. This data is downloaded from data.gov.sg. There are two versions. One in ESRI shapefile format. The other one in Google kml file format. For the purpose of this hands-on exercise, ESRI shapefile format is provided.\n\n- OD_Matrix: a distance matrix in csv format. There are six fields in the data file. They are:\n  - origin_id: the unique id values of the origin (i.e. fid of hexagon data set)\n  - destination_id: the unique id values of the destination (i.e. fid of ELDERCARE data set)\n  - entry_cost: the perpendicular distance between the origins and the nearest road\n  - network_cost: the actual network distance from the origin and destination\n  - exit_cost: the perpendicular distance between the destination and the nearest road\n  - total_cost: the summation of entry_cost, network_cost and exit_cost\n  \n## Getting Started\nBefore we getting started, it is important for us to install the necessary R packages and launch them into RStudio environment.\nThe code chunk below installs and launches these R packages into RStudio environment.\n\n::: {.cell}\n\n```{.r .cell-code}\npacman::p_load(tmap, SpatialAcc, sf, \n               ggstatsplot, reshape2,\n               tidyverse)\n```\n:::\n\n## Geospatial Data Wrangling \n### Importing geospatial data\nThree geospatial data will be imported from the data/geospatial sub-folder. They are MP14_SUBZONE_NO_SEA_PL, hexagons and ELDERCARE.\n\nThe code chunk below is used to import these three data sets shapefile by using st_read() of sf packages.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmpsz <- st_read(dsn = \"data/geospatial\", layer = \"MP14_SUBZONE_NO_SEA_PL\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nReading layer `MP14_SUBZONE_NO_SEA_PL' from data source \n  `C:\\govanzz\\ISSS626_GEO_AUG_2025\\In-class_Exercise\\In-class_Ex09\\data\\geospatial' \n  using driver `ESRI Shapefile'\nSimple feature collection with 323 features and 15 fields\nGeometry type: MULTIPOLYGON\nDimension:     XY\nBounding box:  xmin: 2667.538 ymin: 15748.72 xmax: 56396.44 ymax: 50256.33\nProjected CRS: SVY21\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagons <- st_read(dsn = \"data/geospatial\", layer = \"hexagons\") \n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nReading layer `hexagons' from data source \n  `C:\\govanzz\\ISSS626_GEO_AUG_2025\\In-class_Exercise\\In-class_Ex09\\data\\geospatial' \n  using driver `ESRI Shapefile'\nSimple feature collection with 3125 features and 6 fields\nGeometry type: POLYGON\nDimension:     XY\nBounding box:  xmin: 2667.538 ymin: 21506.33 xmax: 50010.26 ymax: 50256.33\nProjected CRS: SVY21 / Singapore TM\n```\n\n\n:::\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\neldercare <- st_read(dsn = \"data/geospatial\", layer = \"ELDERCARE\") \n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nReading layer `ELDERCARE' from data source \n  `C:\\govanzz\\ISSS626_GEO_AUG_2025\\In-class_Exercise\\In-class_Ex09\\data\\geospatial' \n  using driver `ESRI Shapefile'\nSimple feature collection with 120 features and 19 fields\nGeometry type: POINT\nDimension:     XY\nBounding box:  xmin: 14481.92 ymin: 28218.43 xmax: 41665.14 ymax: 46804.9\nProjected CRS: SVY21 / Singapore TM\n```\n\n\n:::\n:::\n\nThe report above shows that the R object used to contain the imported MP14_SUBZONE_WEB_PL shapefile is called mpsz and it is a simple feature object. The geometry type is multipolygon. it is also important to note that mpsz simple feature object does not have EPSG information.\n\n### Updating CRS information\nThe code chunk below updates the newly imported mpsz with the correct ESPG code (i.e. 3414)\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmpsz <- st_transform(mpsz, 3414)\neldercare <- st_transform(eldercare, 3414)\nhexagons <- st_transform(hexagons, 3414)\n```\n:::\n\n\nThe code chunk below will be used to verify the newly transformed mpsz_svy21.\n\n::: {.cell}\n\n```{.r .cell-code}\nst_crs(mpsz)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nCoordinate Reference System:\n  User input: EPSG:3414 \n  wkt:\nPROJCRS[\"SVY21 / Singapore TM\",\n    BASEGEOGCRS[\"SVY21\",\n        DATUM[\"SVY21\",\n            ELLIPSOID[\"WGS 84\",6378137,298.257223563,\n                LENGTHUNIT[\"metre\",1]]],\n        PRIMEM[\"Greenwich\",0,\n            ANGLEUNIT[\"degree\",0.0174532925199433]],\n        ID[\"EPSG\",4757]],\n    CONVERSION[\"Singapore Transverse Mercator\",\n        METHOD[\"Transverse Mercator\",\n            ID[\"EPSG\",9807]],\n        PARAMETER[\"Latitude of natural origin\",1.36666666666667,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8801]],\n        PARAMETER[\"Longitude of natural origin\",103.833333333333,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8802]],\n        PARAMETER[\"Scale factor at natural origin\",1,\n            SCALEUNIT[\"unity\",1],\n            ID[\"EPSG\",8805]],\n        PARAMETER[\"False easting\",28001.642,\n            LENGTHUNIT[\"metre\",1],\n            ID[\"EPSG\",8806]],\n        PARAMETER[\"False northing\",38744.572,\n            LENGTHUNIT[\"metre\",1],\n            ID[\"EPSG\",8807]]],\n    CS[Cartesian,2],\n        AXIS[\"northing (N)\",north,\n            ORDER[1],\n            LENGTHUNIT[\"metre\",1]],\n        AXIS[\"easting (E)\",east,\n            ORDER[2],\n            LENGTHUNIT[\"metre\",1]],\n    USAGE[\n        SCOPE[\"Cadastre, engineering survey, topographic mapping.\"],\n        AREA[\"Singapore - onshore and offshore.\"],\n        BBOX[1.13,103.59,1.47,104.07]],\n    ID[\"EPSG\",3414]]\n```\n\n\n:::\n:::\n\n\n###  Cleaning and updating attribute fields of the geospatial data\nThere are many redundant fields in the data tables of both eldercare and hexagons. The code chunks below will be used to exclude those redundant fields. At the same time, a new field called demand and a new field called capacity will be added into the data table of hexagons and eldercare sf data frame respectively. Both fields are derive using mutate() of dplyr package.\n\n::: {.cell}\n\n```{.r .cell-code}\neldercare <- eldercare %>%\n  select(fid, ADDRESSPOS) %>%\n  mutate(capacity = 100)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagons <- hexagons %>%\n  select(fid) %>%\n  mutate(demand = 100)\n```\n:::\n\n\nNotice that for the purpose of this hands-on exercise, a constant value of 100 is used. In practice, actual demand of the hexagon and capacity of the eldercare centre should be used.\n\n## Apsaital Data Handling and Wrangling\n\n### Importing Distance Matrix\n\nThe code chunk below uses read_cvs() of readr package to import OD_Matrix.csv into RStudio. The imported object is a tibble data.frame called ODMatrix.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nODMatrix <- read_csv(\"data/aspatial/OD_Matrix.csv\", skip = 0)\n```\n:::\n\n\nThe code chunk below uses spread() of tidyr package is used to transform the O-D matrix from a thin format into a fat format.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndistmat <- ODMatrix %>%\n  select(origin_id, destination_id, total_cost) %>%\n  spread(destination_id, total_cost)%>%\n  select(c(-c('origin_id')))\n```\n:::\n\n\nCurrently, the distance is measured in metre because SVY21 projected coordinate system is used. The code chunk below will be used to convert the unit f measurement from metre to kilometre.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndistmat_km <- as.matrix(distmat/1000)\n```\n:::\n\n\n## Modelling and Visualising Accessibility using Hansen Method\n\n###  Computing Hansen’s accessibility power 0.5\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_1 <- data.frame(ac(hexagons$demand,\n                            eldercare$capacity,\n                            distmat_km, \n                            #d0 = 50,\n                            power = 0.5, \n                            family = \"Hansen\"))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolnames(acc_Hansen_1) <- \"accHansen_1\"\n```\n:::\n\nNext, we will convert the data table into tibble format by using the code chunk below.\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_1 <- as_tibble(acc_Hansen_1)\n```\n:::\n\n\nLastly, bind_cols() of dplyr will be used to join the acc_Hansen tibble data frame with the hexagons simple feature data frame. The output is called hexagon_Hansen.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagon_Hansen_1 <- bind_cols(hexagons, acc_Hansen_1)\n```\n:::\n\n\n###  Computing Hansen’s accessibility power 1\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_2 <- data.frame(ac(hexagons$demand,\n                            eldercare$capacity,\n                            distmat_km, \n                            #d0 = 50,\n                            power = 1, \n                            family = \"Hansen\"))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolnames(acc_Hansen_2) <- \"accHansen_2\"\n```\n:::\n\nNext, we will convert the data table into tibble format by using the code chunk below.\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_2 <- as_tibble(acc_Hansen_2)\n```\n:::\n\n\nLastly, bind_cols() of dplyr will be used to join the acc_Hansen tibble data frame with the hexagons simple feature data frame. The output is called hexagon_Hansen.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagon_Hansen_2 <- bind_cols(hexagons, acc_Hansen_2)\n```\n:::\n\n\n\n###  Computing Hansen’s accessibility power 1.5\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_3 <- data.frame(ac(hexagons$demand,\n                            eldercare$capacity,\n                            distmat_km, \n                            #d0 = 50,\n                            power = 1.5, \n                            family = \"Hansen\"))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolnames(acc_Hansen_3) <- \"accHansen_3\"\n```\n:::\n\nNext, we will convert the data table into tibble format by using the code chunk below.\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_3 <- as_tibble(acc_Hansen_3)\n```\n:::\n\n\nLastly, bind_cols() of dplyr will be used to join the acc_Hansen tibble data frame with the hexagons simple feature data frame. The output is called hexagon_Hansen.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagon_Hansen_3 <- bind_cols(hexagons, acc_Hansen_3)\n```\n:::\n\n\n###  Computing Hansen’s accessibility power 2\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_4 <- data.frame(ac(hexagons$demand,\n                            eldercare$capacity,\n                            distmat_km, \n                            #d0 = 50,\n                            power = 2, \n                            family = \"Hansen\"))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolnames(acc_Hansen_4) <- \"accHansen_4\"\n```\n:::\n\nNext, we will convert the data table into tibble format by using the code chunk below.\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_4 <- as_tibble(acc_Hansen_4)\n```\n:::\n\n\nLastly, bind_cols() of dplyr will be used to join the acc_Hansen tibble data frame with the hexagons simple feature data frame. The output is called hexagon_Hansen.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagon_Hansen_4 <- bind_cols(hexagons, acc_Hansen_4)\n```\n:::\n\n\n###  Computing Hansen’s accessibility power 2.5\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_5 <- data.frame(ac(hexagons$demand,\n                            eldercare$capacity,\n                            distmat_km, \n                            #d0 = 50,\n                            power = 2.5, \n                            family = \"Hansen\"))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolnames(acc_Hansen_5) <- \"accHansen_5\"\n```\n:::\n\nNext, we will convert the data table into tibble format by using the code chunk below.\n\n::: {.cell}\n\n```{.r .cell-code}\nacc_Hansen_5 <- as_tibble(acc_Hansen_5)\n```\n:::\n\n\nLastly, bind_cols() of dplyr will be used to join the acc_Hansen tibble data frame with the hexagons simple feature data frame. The output is called hexagon_Hansen.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhexagon_Hansen_5 <- bind_cols(hexagons, acc_Hansen_5)\n```\n:::\n\n\n## Visualisation\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(dplyr)\nlibrary(tidyr)\nlibrary(tmap)\nlibrary(sf)\n\n# 1) Combine the five columns onto hexagons (reuse what you already made)\nhexagon_all <- hexagons %>%\n  bind_cols(\n    acc_Hansen_1,  # col accHansen_1\n    acc_Hansen_2,  # col accHansen_2\n    acc_Hansen_3,  # col accHansen_3\n    acc_Hansen_4,  # col accHansen_4\n    acc_Hansen_5   # col accHansen_5\n  )\n\n# 2) Go long so each power becomes a facet\nhexagon_long <- hexagon_all %>%\n  pivot_longer(\n    cols = starts_with(\"accHansen_\"),\n    names_to = \"power\",\n    values_to = \"acc\"\n  ) %>%\n  mutate(power = factor(power,\n                        levels = c(\"accHansen_1\",\"accHansen_2\",\"accHansen_3\",\"accHansen_4\",\"accHansen_5\"),\n                        labels = c(\"power = 0.5\",\"power = 1\",\"power = 1.5\",\"power = 2\",\"power = 2.5\")))\n\n# 3) Build global breaks for a shared color scale\nvals <- hexagon_long$acc\nbrks <- quantile(vals, probs = seq(0, 1, length.out = 11), na.rm = TRUE)\n\n# 4) Plot as facets with one legend\ntmap_mode(\"plot\")\nmapex <- st_bbox(hexagons)\n\ntm_shape(hexagon_long, bbox = mapex) +\n  tm_polygons(\n    col = \"acc\",\n    fill.scale = tm_scale_continuous(breaks = brks, values = \"brewer.blues\"),\n    title = \"Hansen accessibility\"\n  ) +\n  tm_facets(by = \"power\", ncol = 3, free.scales = FALSE) +\n  tm_layout(legend.outside = TRUE, frame = TRUE) +\n  tm_title(\"Accessibility to eldercare (Hansen) across impedance powers\") +\n  tm_compass(type = \"8star\", size = 1.5) +\n  tm_scalebar() +\n  tm_grid(alpha = 0.2)\n```\n\n::: {.cell-output-display}\n![](In-class_Ex09_files/figure-html/unnamed-chunk-32-1.png){width=672}\n:::\n:::\n\n\n\n\n",
    "supporting": [
      "In-class_Ex09_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}